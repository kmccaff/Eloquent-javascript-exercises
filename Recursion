function IsEven(number) {
    if (number === 0) {
        return true;
    }  else if (number == 1) {
       return false;
    } else if (number < 0) {
      return IsEven(number + 2);
    } else {
    console.log(number);
    return IsEven(number - 2);
    }
}
