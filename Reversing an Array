function reverseArrayInPlace(x) {
  for ( var i = x.length; i >= 0; i-- ) {
    x.push(x[i]);
    x.splice(i, 1)
  }
}

function reverseArray(x) {
  var reversedArray = [];
  for ( var i = x.length - 1; i >= 0; i-- ) {
      var pos = x[i]
    reversedArray.push(pos)
  }
  return reversedArray;
}
